// The lifted source reads BACKEND_BASE_URL; this app configures the same
// backend under the source's own key, GRC_PLATFORM_BACKEND_BASE_URL.
export { securityBackendUrl as BACKEND_BASE_URL } from "@config/apiConfig";
